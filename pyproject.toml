[project]
name = "stable-diffusion-modal-deploy"
version = "1.0.0"
description = "Deploy SD onto modal platform using uv"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "modal>=1.3.5",
]
